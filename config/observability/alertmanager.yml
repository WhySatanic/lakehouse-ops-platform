global:
  resolve_timeout: 1m

route:
  receiver: core-webhook
  group_by: [alertname, instance]
  group_wait: 5s
  group_interval: 30s
  repeat_interval: 4h

receivers:
  - name: core-webhook
    webhook_configs:
      - url: http://alert-webhook:8080/alerts
        send_resolved: true
